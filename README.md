# NOCTURNE

```
npm install
npm run dev
```
Vite + React + TypeScript + Tailwind v4 + Framer Motion. Bottles and photography are SVG/CSS placeholders (see components/Bottle.tsx, Reveal.tsx > Atmos); swap in real campaign images there.
